╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║          DOCKER DEPLOYMENT - QUICK REFERENCE CARD             ║
║          Rawlings Global Solutions                            ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝

═════════════════════════════════════════════════════════════════
  PREREQUISITES
═════════════════════════════════════════════════════════════════

  1. Install Docker Desktop
     → https://www.docker.com/products/docker-desktop
     → Windows 10/11 (64-bit) required
     → At least 4GB RAM recommended
     → Restart computer after installation

  2. Start Docker Desktop
     → Look for whale icon in system tray
     → Wait until "Docker Desktop is running"

═════════════════════════════════════════════════════════════════
  QUICK START (ONE-CLICK METHOD)
═════════════════════════════════════════════════════════════════

  DEVELOPMENT MODE (with hot reload):
    1. Double-click: docker-start.bat
    2. Choose option: 1
    3. Wait 2-3 minutes
    4. Open: http://localhost:3000

  PRODUCTION MODE (optimized):
    1. Double-click: docker-start.bat
    2. Choose option: 2
    3. Wait 3-5 minutes
    4. Open: http://localhost:3000

  STOP CONTAINERS:
    → Double-click: docker-stop.bat

═════════════════════════════════════════════════════════════════
  MANUAL COMMANDS
═════════════════════════════════════════════════════════════════

  START DEVELOPMENT:
    docker-compose up dev

  START PRODUCTION:
    docker-compose up --build prod

  RUN IN BACKGROUND:
    docker-compose up -d dev
    docker-compose up -d prod

  VIEW LOGS:
    docker-compose logs -f dev
    docker-compose logs -f prod

  STOP ALL:
    docker-compose down

  RESTART:
    docker-compose restart dev
    docker-compose restart prod

═════════════════════════════════════════════════════════════════
  DOCKER COMMANDS CHEAT SHEET
═════════════════════════════════════════════════════════════════

  CONTAINERS:
    docker ps                    # List running containers
    docker ps -a                 # List all containers
    docker stop <container>      # Stop container
    docker start <container>     # Start container
    docker restart <container>   # Restart container
    docker rm <container>        # Remove container
    docker logs <container>      # View logs
    docker exec -it <name> sh    # Enter container shell

  IMAGES:
    docker images                # List images
    docker rmi <image>           # Remove image
    docker build -t name .       # Build image

  CLEANUP:
    docker system prune          # Remove unused data
    docker system prune -a       # Remove all unused data
    docker volume prune          # Remove unused volumes
    docker image prune           # Remove unused images

═════════════════════════════════════════════════════════════════
  FILE STRUCTURE
═════════════════════════════════════════════════════════════════

  Dockerfile              → Production build (multi-stage)
  Dockerfile.dev          → Development build (hot reload)
  docker-compose.yml      → Orchestration config
  .dockerignore           → Exclude files from build
  docker-start.bat        → Quick launcher script
  docker-stop.bat         → Stop script
  DOCKER.md               → Complete documentation

═════════════════════════════════════════════════════════════════
  ACCESSING THE APP
═════════════════════════════════════════════════════════════════

  FROM YOUR COMPUTER:
    http://localhost:3000

  FROM ANOTHER DEVICE (same network):
    1. Find your IP: ipconfig
    2. Use: http://YOUR_IP:3000
       Example: http://192.168.1.100:3000

  FROM MOBILE (same WiFi):
    → Same as above: http://YOUR_IP:3000

═════════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═════════════════════════════════════════════════════════════════

  DOCKER NOT STARTING:
    → Restart computer
    → Enable virtualization in BIOS
    → Update WSL: wsl --update

  PORT 3000 IN USE:
    → Stop other services
    → Or change port in docker-compose.yml:
      ports: ["3001:3000"]

  BUILD FAILS:
    → Clear cache: docker builder prune
    → Increase memory in Docker Desktop settings
    → Rebuild: docker-compose up --build

  HOT RELOAD NOT WORKING:
    → Restart: docker-compose restart dev
    → Rebuild: docker-compose up --build dev

  CANNOT ACCESS FROM OTHER DEVICE:
    → Check both on same network
    → Allow port 3000 in Windows Firewall
    → Verify container is running: docker ps

═════════════════════════════════════════════════════════════════
  DEVELOPMENT WORKFLOW
═════════════════════════════════════════════════════════════════

  1. Start dev container:
     docker-compose up dev

  2. Edit files (they're synced automatically)
     → Open: lib/content.ts
     → Make changes
     → Save (Ctrl+S)
     → Browser updates automatically ✨

  3. View logs in terminal
     → See real-time updates
     → Spot errors immediately

  4. Stop when done:
     Ctrl+C or docker-compose down

═════════════════════════════════════════════════════════════════
  PRODUCTION DEPLOYMENT
═════════════════════════════════════════════════════════════════

  LOCAL TESTING:
    docker-compose up prod

  BUILD FOR DEPLOYMENT:
    docker build -t rgs-webapp:latest .

  TAG IMAGE:
    docker tag rgs-webapp username/rgs-webapp:v1.0

  PUSH TO REGISTRY:
    docker push username/rgs-webapp:v1.0

  PULL ON SERVER:
    docker pull username/rgs-webapp:v1.0
    docker run -p 3000:3000 username/rgs-webapp:v1.0

═════════════════════════════════════════════════════════════════
  USEFUL CHECKS
═════════════════════════════════════════════════════════════════

  CHECK DOCKER RUNNING:
    docker ps

  CHECK LOGS:
    docker-compose logs

  CHECK RESOURCE USAGE:
    docker stats

  CHECK CONTAINER DETAILS:
    docker inspect rgs-webapp-dev

  ENTER CONTAINER:
    docker exec -it rgs-webapp-dev sh

═════════════════════════════════════════════════════════════════
  PERFORMANCE TIPS
═════════════════════════════════════════════════════════════════

  ✨ Use docker-compose for easier management
  ✨ Keep Docker Desktop running for faster startups
  ✨ Allocate 4GB+ RAM to Docker in settings
  ✨ Clean up regularly: docker system prune
  ✨ Use production mode for better performance
  ✨ Enable BuildKit for faster builds:
     set DOCKER_BUILDKIT=1

═════════════════════════════════════════════════════════════════
  COMPARISON: DEV VS PROD
═════════════════════════════════════════════════════════════════

  DEVELOPMENT MODE:
    ✓ Hot reload enabled
    ✓ Full dev dependencies
    ✓ Source maps
    ✓ Faster iteration
    ✗ Larger image (~1GB)
    ✗ Slower performance

  PRODUCTION MODE:
    ✓ Optimized build
    ✓ Small image (~150MB)
    ✓ Fast performance
    ✓ Secure (non-root user)
    ✗ No hot reload
    ✗ Rebuild needed for changes

═════════════════════════════════════════════════════════════════
  BEFORE YOU START
═════════════════════════════════════════════════════════════════

  ✓ Docker Desktop installed and running
  ✓ At least 4GB free RAM
  ✓ Virtualization enabled
  ✓ Internet connection for first build
  ✓ Port 3000 available

═════════════════════════════════════════════════════════════════
  GETTING HELP
═════════════════════════════════════════════════════════════════

  📖 Full documentation: DOCKER.md
  📖 Docker docs: https://docs.docker.com/
  📖 Next.js Docker: https://nextjs.org/docs/deployment#docker
  📖 Troubleshooting: See DOCKER.md section

═════════════════════════════════════════════════════════════════

🐳 Docker Setup Complete! Use docker-start.bat to begin.

═════════════════════════════════════════════════════════════════
