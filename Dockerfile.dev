# Rawlings Global Solutions - Development Dockerfile
# For local development with hot reload

FROM node:18-alpine

WORKDIR /app

# Install dependencies for node-gyp (if needed)
RUN apk add --no-cache libc6-compat

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy source code
COPY . .

# Expose port 3000
EXPOSE 3000

# Set environment
ENV NODE_ENV development

# Start development server with hot reload
CMD ["npm", "run", "dev"]
